namespace Domain
{
    public static class CacheKeys
    {
        public const string CURRENT_JOB_ID = nameof(CURRENT_JOB_ID);
        public const string CURRENT_RUNNING_JOBS = nameof(CURRENT_RUNNING_JOBS);
    }
}